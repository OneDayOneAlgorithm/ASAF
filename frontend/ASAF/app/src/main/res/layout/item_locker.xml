<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="100dp"
    android:layout_height="match_parent"
    android:layout_marginBottom="5dp"
    >

    <ImageView
        android:id="@+id/item_locker_imageview_image"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:src="@drawable/vault"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/item_locker_imageview_text"
        tools:src="@drawable/vault" />

    <TextView
        android:id="@+id/item_locker_imageview_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="24sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/item_locker_imageview_image"
        app:layout_constraintStart_toStartOf="@+id/item_locker_imageview_image"
        app:layout_constraintTop_toBottomOf="@+id/item_locker_imageview_image"
        tools:text="박현우" />

</androidx.constraintlayout.widget.ConstraintLayout>